const b=document.getElementById('board'),s=document.getElementById('score');let score=0,g=[...Array(64)].map(_=>0);for(let i=0;i<64;i++){let d=document.createElement('div');d.className='cell';d.onclick=()=>place(i);b.appendChild(d)}function draw(){[...b.children].forEach((c,i)=>c.className='cell'+(g[i]?' fill':''));s.textContent=score}function place(i){if(g[i])return;g[i]=1;score+=10;let r=Math.floor(i/8);if(g.slice(r*8,r*8+8).every(v=>v)){for(let k=r*8;k<r*8+8;k++)g[k]=0;score+=80}draw()}draw();